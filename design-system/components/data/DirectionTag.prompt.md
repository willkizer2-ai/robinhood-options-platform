A CALL/PUT chip with a directional caret — teal for CALL (up), magenta for PUT (down). Use anywhere a trade's direction is shown.

```jsx
<DirectionTag direction="CALL" />
<DirectionTag direction="PUT" size="sm" />
```
