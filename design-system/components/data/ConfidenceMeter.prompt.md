A confidence percentage (0–100) with a thin color-stepped bar — teal when high, periwinkle mid, gold/muted low. Use on trade cards and setup rows.

```jsx
<ConfidenceMeter label="Confidence" value={78} width={140} />
```
